# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @zipzap/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t zipzap-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system zipzap-auth-service && \
          adduser --system -G zipzap-auth-service zipzap-auth-service

COPY apps/auth-service/dist zipzap-auth-service/
COPY apps/auth-service/package.json zipzap-auth-service/
RUN chown -R zipzap-auth-service:zipzap-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix zipzap-auth-service --omit=dev -f install

CMD [ "node", "zipzap-auth-service" ]
